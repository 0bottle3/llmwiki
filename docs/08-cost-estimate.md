# 08. 비용 추정

전제: 10명 팀, 월 신규 1,000개 문서, 월 검색 10,000회, ap-northeast-2.

## 한눈에 보는 월 비용

| 항목 | 시나리오 A (관리형) | 시나리오 B (자체 호스팅) |
|------|---------------------|-------------------------|
| EKS 노드 (한계 비용) | ~$60 | ~$90 |
| OpenSearch Serverless | ~$170 | $0 (Qdrant on EKS) |
| S3 | ~$5 | ~$5 |
| SQS / CloudWatch | ~$5 | ~$5 |
| Anthropic Claude (가공) | ~$30~80 | ~$30~80 |
| Anthropic Claude (검색 리랭킹) | ~$10 | ~$10 |
| Voyage 임베딩 | ~$5~10 | ~$5~10 |
| ALB | ~$25 | ~$25 |
| NAT (외부 LLM 호출) | ~$30 | ~$30 |
| Secrets Manager / KMS | ~$5 | ~$5 |
| **합계** | **~$345~400** | **~$205~260** |

EKS 클러스터 자체 비용($72/월)은 이미 운영 중이라고 가정하여 제외.

## 시나리오별 산정 근거

### EKS 노드 (한계 비용)

- search-api: 2 replica × 512Mi → 기존 노드 여유 활용
- processor: KEDA로 평소 0, 가공 시 burst → 2~3노드 × t3.medium 1~2시간/일
- ingestor/curator: 무시 가능
- 한계 비용: 노드 0.5대 상시 + burst → **$60~90/월**

### OpenSearch Serverless

- 최소 2 OCU (Indexing 1 + Search 1) = `0.24/OCU·시간 × 2 × 730시간 ≈ $350`
- 단, AOSS는 idle 시 자동 0.5 OCU로 떨어짐 (실제 청구 더 적음)
- 보수적으로 **$170/월**
- 데이터 양 늘면 +Storage ($0.024/GB·월)

### Qdrant on EKS (대안)

- 1~2 Pod, 512Mi~2Gi
- 기존 노드에 흡수 가능 → **$0 추가**
- 운영 부담 + 가용성은 사용자 책임

### S3

- 원본 + 가공본 + 임베딩 ≈ 50GB
- Standard: $0.025/GB·월 = $1.25
- 요청 (PUT/GET): ~$2
- Glacier IR 전환 후 더 낮아짐
- **$5/월**

### SQS / CloudWatch

- SQS: 100만 요청 무료 후 $0.40/100만
- CloudWatch Logs: 5GB/월 ~$2.5
- 메트릭: 10개 커스텀 ~$3
- **$5/월**

### Anthropic Claude (가공)

전제:
- 신규 문서 1,000개 × 평균 3,000 토큰 input + 1,000 출력
- Haiku 4.5 ($0.80/1M in, $4/1M out) + Batch API 50% + Prompt Caching 90%

```
입력 토큰: 1,000 × 3,000 = 3M
  - 캐시 적중 70%: 2.1M × $0.08 (cached) = $0.17
  - 캐시 미적중 30%: 0.9M × $0.80 = $0.72
출력 토큰: 1,000 × 1,000 = 1M × $4 = $4
배치 50% 할인: ($0.89 + $4) × 0.5 = $2.45
```

여기에 큐레이션/품질 점수/병합 판정 등 추가 호출 → 보수적으로 **$30~80/월**.

### Anthropic Claude (검색 리랭킹, 옵션)

- 월 10,000 검색 × 평균 5,000 토큰 (top-20 후보)
- Haiku: 50M × $0.80 = $40
- 캐싱 미적용 (쿼리마다 다름) → 비싸 보임
- 리랭킹은 **선택**, 안 써도 OK
- 쓸 거면 **$10~40/월**

### Voyage 임베딩

- 신규 문서 1,000개 × 1,000 토큰 = 1M tokens × $0.06 = $0.06
- 쿼리 임베딩 10,000 × 50 토큰 = 0.5M × $0.06 = $0.03
- 재인덱싱(분기 1회): 전체 ~ 5M tokens × $0.06 = $0.30
- **$5~10/월** (여유)

### ALB

- 시간당 $0.0225 × 730시간 = $16.4
- LCU 사용 ~$5
- **$25/월**

### NAT Gateway

- 외부 LLM API 호출용
- 시간당 $0.045 × 730 = $33
- 데이터: 토큰 트래픽 작아서 $1 미만
- VPC Endpoint Interface로 일부 대체 → **$30/월**

### Secrets Manager / KMS

- 시크릿 ~10개 × $0.40 = $4
- API 호출: 무시 가능
- KMS 키 1개 × $1 + 호출 ~$0.5
- **$5/월**

## 시나리오 변화 시 영향

### 팀 규모 2배 (20명)

| 변화 | 영향 |
|------|------|
| 문서 2배 | LLM 비용 ~2배 = +$50 |
| 검색 2배 | 리랭킹 +$10 |
| 가공 노드 burst 늘어남 | +$30 |
| **총 증분** | **+$90 → $440** |

### 문서 10배 (10,000/월)

| 변화 | 영향 |
|------|------|
| LLM 가공 | $300~800 |
| Voyage 임베딩 | $50~100 |
| OpenSearch 스토리지 | +$10 |
| processor 노드 | +$100 |
| **총** | **$800~1,300** |

여전히 사람 1명 인건비보다 한참 싼 수준.

## 비용 절감 레버

1. **Prompt Caching 무조건 켜기** — 70% 이상 절감
2. **Batch API 우선** — 50% 추가 할인, 실시간성 거의 필요 없음
3. **Haiku 최우선** — 가공 품질 충분, Sonnet은 검증/리랭킹만
4. **KEDA로 processor 0→N** — idle 시 노드 0
5. **OpenSearch Serverless → Qdrant** — 월 $170 절감 (운영 부담 trade-off)
6. **NAT → VPC Endpoint** — LLM API용은 Anthropic이 PrivateLink 미지원이라 한계
7. **embedding 모델 차원 줄이기** — Voyage 1024 → 512 (저장/RAM 절반)

## 비용 모니터링

```
CloudWatch Alarms:
  - 일일 LLM 토큰 사용량 > 임계
  - 일일 S3 객체 수 > 임계
  - 월 OpenSearch OCU > 3
  - SQS DLQ depth > 0

월말 자동 리포트:
  - curator가 토큰 사용량 × 단가로 모델별 비용 추정
  - 가장 비싼 사용자/주제 top 10
  - Prompt cache hit rate
```

## 결론

- **MVP**: 시나리오 B (Qdrant) → 월 ~$200대
- **운영 안정 후**: 시나리오 A (OpenSearch) → 월 ~$350~400
- 10명 팀이면 1인당 $20~40/월 = 노션 비즈니스($24)보다 약간 비싸지만
  **AI 통합 + 무한 LLM 사용**까지 합치면 압도적 가성비
